<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <!-- Background color -->
    <solid android:color="@color/uber_orange" />

    <!-- Rounded corners -->
    <corners android:radius="16dp" />

    <!-- Border / stroke -->
    <stroke
        android:width="2dp"
        android:color="@color/white" />
</shape>
